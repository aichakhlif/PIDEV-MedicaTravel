{% extends 'form.html.twig' %}
{% block body %}


        <div class="content mt-3">
            <div class="animated fadeIn">
                <div class="row">

                    <div class="col-md-12">
                        <div class="card">
                            <div class="card-header">
                                <strong class="card-title">Data Table</strong>
                            </div>
                            <div class="card-body">
                                <table id="bootstrap-data-table-export" class="table table-striped table-bordered">

                                    <tr>

                                        <td> ID </td>
                                        <td>Text</td>

                                    </tr>
                                    {% for r in listreclamation %}

                                    <tr>
                                        <td> {{r.id}}</td>
                                        <td> {{r.text}}</td>
                                        <td> {{r.patient}}</td>
                                        <td><a href="{{ path('deleteR',{'id':r.id}) }}">EFFACER</a></td>


                                    </tr>

                                    {% endfor %}
                                </table>
                            </div>
                        </div>
                    </div>


                </div>
            </div><-- .animated -->
        </div><-- .content -->

    {% endblock %}