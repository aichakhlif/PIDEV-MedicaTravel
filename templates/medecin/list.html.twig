{% extends 'base.html.twig' %}
{% block body %}

    <html>
    <style>
        .animateuse{
            animation: leelaanimate 2s infinite;
        }
        @keyframes leelaanimate{
            0%{color:red}
            10%{color:Grey}
            20%{color:whitesmoke}
        }</style>
    <div class="content mt-3">
        <div class="animated fadeIn">
            <div class="row">

                <div class="col-md-12">
                    <div class="card">
                        <div class="card-header">
                            <strong class="card-title"><h1 class="text-center   text-uppercase animateuse">Liste des medecins !</h1></strong>
                        </div>
                        <div class="card-body">
                            <table id="bootstrap-data-table-export" class="table table-striped table-bordered">
                                <tr>
                                <tr>
                                    <td>ID</td>
                                    <td>Nom</td>
                                    <td></td>



                                </tr>
                                {% for med in med %}
                                    <tr>
                                        <td>{{med.id}}</td>
                                        <td>{{med.nom}}</td>
                                        <td><a href="{{ path('dropmed' ,{'id':med.id})}}" ><button type="button" class="btn btn-outline-danger" data-dismiss="modal">Delete</button></a></td>



                                    </tr>

                                {%endfor%}
                            </table>
                        </div>
                    </div>
                </div>


            </div>
        </div><!-- .animated -->
    </div><!-- .content -->


    </div><!-- /#right-panel -->

    </html>
{% endblock %}