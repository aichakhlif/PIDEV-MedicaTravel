{% extends 'base.html.twig' %}
{% block body %}

    <html>
    <style>
        .animateuse{
            animation: leelaanimate 2s infinite;
        }
        @keyframes leelaanimate{
            0%{color:red}
            10%{color:Grey}
            20%{color:whitesmoke}
        }</style>
    <div class="content mt-3">
        <div class="animated fadeIn">
            <div class="row">

                <div class="col-md-12">
                    <div class="card">
                        <div class="card-header">
                            <strong class="card-title"><h1 class="text-center   text-uppercase animateuse">Liste des Offres !</h1></strong>
                        </div>
                        <div class="card-body">
                            <table id="bootstrap-data-table-export" class="table table-striped table-bordered">
                                <tr>
                                    <td>ID</td>
                                    <td>Nom Médecin</td>
                                    <td>Nom Clinique</td>
                                    <td>Nom Intervention</td>
                                    <td>Prix</td>
                                    <td></td>
                                    <td></td>



                                </tr>
                                {% for offre in offre %}
                                    <tr>
                                        <td>{{offre.id}}</td>
                                        <td>{{offre.medecin}}</td>
                                        <td>{{offre.clinique}}</td>
                                        <td>{{offre.intervention}}</td>
                                        <td>{{offre.prix}}</td>

                                        <td><a href="{{ path('updateoffre',{'id':offre.id}) }}"><button type="button" class="btn btn-outline-success" data-dismiss="modal">Update</button></a></td>
                                        <td><a href="{{ path('dropoffre',{'id':offre.id}) }}" ><button type="button" class="btn btn-outline-danger" data-dismiss="modal">Delete</button></a></td>



                                    </tr>

                                {%endfor%}
                            </table>
                        </div>
                    </div>
                </div>


            </div>
        </div><!-- .animated -->
    </div><!-- .content -->


    </div><!-- /#right-panel -->
    </html>
{% endblock %}
